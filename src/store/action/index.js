import * as ProductListAction from "../action/ProductListAction";
import * as CartListAction from "../action/cartAction";

export const ActionCreators = Object.assign(
  {},
  ProductListAction,
  CartListAction
);